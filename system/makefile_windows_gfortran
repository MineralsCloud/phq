NAME = phmd.exe

FC = gfortran

OFILES = configure.o main.o phmd.o

${NAME} : ${OFILES}
	${FC} -o ${NAME} ${OFILES} ${LIB}

clean :
	rm -f *.obj *.mod *.exe

configure.o : configure.f90
	${FC} -c $<

main.o : main.f90 
	${FC} -c $<

phmd.o : phmd.f90 
	${FC} -c $<
